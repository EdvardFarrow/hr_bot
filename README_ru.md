# 🤖 AI HR Recruiter Bot

[![En](https://img.shields.io/badge/lang-en-red.svg)](README.md)
![Coverage](./coverage.svg)
![Python](https://img.shields.io/badge/python-3.12-blue.svg)
![Aiogram](https://img.shields.io/badge/Aiogram-3.x-2ca5e0.svg)
![Gemini](https://img.shields.io/badge/AI-Gemini_2.5-purple.svg)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ed.svg)

Этот проект показывает, как создать **проактивного AI-рекрутера**, который умеет читать резюме, понимать контекст и проводить первичный отсев кандидатов.

Вместо линейной логики здесь работает **Google Gemini 2.5**, превращая бота из "автоответчика" в полноценного ассистента.

---

## Особенности

* **Чтение файлов:** Бот вытаскивает текст из PDF-резюме, сверяет стек кандидата с вакансией и дает мгновенный фидбек (подходит/не подходит).
* **Чтение ссылок:** Кандидат скинул ссылку на портфолио или профиль? Бот перейдет по ней, спарсит контент и проанализирует его.
* **Помнить контекст:** Благодаря **Redis**, диалог не сбрасывается. Бот помнит, что вы скинули резюме минуту назад, и готов отвечать на вопросы по нему.
* **Профессионализм:** Бот вежливо уходит от вопросов про зарплату/плюшки ("Обсудим на интервью") и фокусирует диалог на технических скиллах.
* **Надежность:** Чистый Python 3.12, строгая типизация, Docker и Poetry.

## Стек технологий

* **Core:** Python 3.12, Aiogram 3 (Asyncio)
* **AI:** Google Gemini 2.5 (via `google-generativeai`)
* **Infrastructure:** Docker, Docker Compose
* **Storage:** Redis (хранение состояний диалога и кэш)
* **Tools:** Poetry, Pydantic, Makefile, Pytest

---

## Запуск

Всё автоматизировано через `Makefile`.

### Обязательные условия
1. Docker и Docker Compose.
2. Токен бота от @BotFather.
3. API ключ Google Gemini.

### 1. Настройка
Создайте файл `.env` в корне проекта (возьмите пример из `.env.example`):

```ini
BOT_TOKEN=ваш_токен
GOOGLE_API_KEY=ваш_ключ
REDIS_HOST=redis
REDIS_PORT=6379
```

### 2. Запуск (Docker) — Рекомендуется

Одна команда для всего:

```bash
make docker-up
```

* **Просмотр логов:** make docker-logs
* **Остановка:** make docker-down

### 3. Запуск (локально)

Если вы хотите запустить его без Docker (требуется локальный Redis):

```bash
make install
make run
```

## Контроль качества

```bash
# Запуск юнит и интеграционных тестов
make test

# Проверка стиля кода (Black + Flake8)
make check
```

## Структура проекта

```plaintext
.
├── app
│   ├── bot          # Логика Telegram
│   ├── services     # Бизнес-логика (AI, Парсер)
│   ├── config.py    # Валидация настроек
│   └── main.py      # Точка входа (Lifespan)
├── tests            # Тесты
├── Dockerfile       # Оптимизированный образ
└── Makefile         # Управление проектом
```